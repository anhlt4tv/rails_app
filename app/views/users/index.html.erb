<p style="color: green"><%= notice %></p>

<h1>Users</h1>
<table>
<thead>
  <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
  </tr>
</thead>
<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.name %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(id: user.id, hung: 123) %></td>
    <td><%= link_to 'Show', user %></td>
  </tr>
<% end %>
</table>
<div>
  <%= link_to "New user", new_user_path %>
</div>